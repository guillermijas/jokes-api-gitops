# frozen_string_literal: true

source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

ruby '3.1.0'

gem 'http'
gem 'puma'
gem 'roda'

# Testing
gem 'rack-test'
gem 'rspec'
